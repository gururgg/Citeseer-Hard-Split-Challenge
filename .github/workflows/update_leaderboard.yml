name: Update Leaderboard

on:
  push:
    branches:
      - main
    paths:
      - 'submissions/**/*.csv'
  workflow_dispatch:

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest
    if: github.repository_owner != 'github-actions[bot]'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
      
    
    - name: Download test labels
      env:
        PRIVATE_Y: ${{ secrets.LABELS }}
        PRIVATE_TEST_MASK_CHALLENGE: ${{ secrets.TEST_MASK_CHALLANGE }}
        PRIVATE_TEST_MASK: ${{ secrets.TEST_MASK }}
    
    - name: Find and evaluate new submissions
      id: evaluate
      run: |
        mkdir -p results
        python scripts/evaluate_all_submissions.py || echo "Evaluation completed with warnings"
        
    - name: Generate leaderboard
      run: |
        # Create initial leaderboard.json if it doesn't exist
        if [ ! -f leaderboard.json ]; then
          echo '{"last_updated": null, "submissions": []}' > leaderboard.json
        fi
        python scripts/generate_leaderboard.py
    
    - name: Commit and push leaderboard
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add leaderboard.json leaderboard.html
        git diff --staged --quiet || git commit -m "Auto-update leaderboard [skip ci]"
        git push || echo "Nothing to push"

